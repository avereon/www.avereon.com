<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

	<modelVersion>4.0.0</modelVersion>

	<parent>
		<groupId>com.parallelsymmetry.pom</groupId>
		<artifactId>top</artifactId>
		<version>1.0.0-SNAPSHOT</version>
	</parent>

	<groupId>com.parallelsymmetry</groupId>
	<artifactId>website</artifactId>
	<version>1.0.0-SNAPSHOT</version>
	<packaging>war</packaging>

	<name>Parallel Symmetry Website</name>
	<description>The Parallel Symmetry website.</description>
	<url>http://www.parallelsymmetry.com/</url>
	<inceptionYear>2008</inceptionYear>

	<repositories>
		<repository>
			<id>psm</id>
			<url>http://code.parallelsymmetry.com/repo/psm</url>
		</repository>
	</repositories>

	<organization>
		<name>Parallel Symmetry</name>
		<url>http://www.parallelsymmetry.com/</url>
	</organization>

	<build>
		<finalName>parallelsymmetry</finalName>
	</build>

	<dependencies>
		<dependency>
			<groupId>javax.servlet</groupId>
			<artifactId>servlet-api</artifactId>
			<version>2.5</version>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>com.parallelsymmetry</groupId>
			<artifactId>terrace</artifactId>
			<version>2.0.0-SNAPSHOT</version>
		</dependency>
	</dependencies>

	<profiles>

		<profile>
			<id>local</id>

			<activation>
				<activeByDefault>true</activeByDefault>
			</activation>

			<build>
				<plugins>

					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>tomcat-maven-plugin</artifactId>
						<version>1.1</version>
						<configuration>
							<url>http://localhost:8080/manager/html</url>
							<server>local-tomcat</server>
							<path>/parasymm</path>
							<warFile>target/${project.build.finalName}.war</warFile>
						</configuration>
					</plugin>

				</plugins>
			</build>
		</profile>

		<profile>
			<id>production</id>

			<build>
				<plugins>

					<plugin>
						<groupId>org.codehaus.mojo</groupId>
						<artifactId>tomcat-maven-plugin</artifactId>
						<version>1.1</version>
						<configuration>
							<url>http://www.parallelsymmetry.com/manager/html</url>
							<server>psm-tomcat</server>
							<path>/</path>
							<warFile>target/${project.build.finalName}.war</warFile>
						</configuration>
					</plugin>

				</plugins>
			</build>
		</profile>

	</profiles>

</project>

